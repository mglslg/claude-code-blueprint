# Claude Code Blueprint

本项目是一个 Claude Code 引导程序（Blueprint）集合，本身不参与任何项目运行，只负责记录和管理各种蓝图。

用户的工作是持续编写各种蓝图，我的任务是协助完成。

## 蓝图编写原则

- 蓝图通过复制粘贴到 Claude Code 对话框使用，驱动 CC 自动构建环境/项目/配置
- 蓝图必须兼容所有模型层级：不仅 Opus 能看懂，Haiku 这类小模型也必须能正确执行
- 指令要极其明确、步骤清晰、无歧义，不给小模型留任何"自由发挥"的空间
- 文件路径、内容、执行顺序都要写死，不能依赖模型的推理能力
